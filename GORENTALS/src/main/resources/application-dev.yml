spring:
  datasource:
    url: jdbc:h2:mem:gorentals;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true

# Keep other settings as per main application.yml
jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000

razorpay:
  key:
    id: rzp_test_mock
    secret: mock_secret

app:
  admin:
    email: admin@gorentals.com
    password: Admin@GoRentals2025!
    name: GoRentals Admin
  frontend-url: http://localhost:3000
